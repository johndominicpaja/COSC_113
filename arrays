DETERMINIGN SUM OF AN ARRAY
-------------------------------------------
import java.util.Scanner;

public class ArraySum {
   public static void main(String[] args) {
      Scanner scnr = new Scanner(System.in);
      final int NUM_ELEMENTS = 8;             // Number of elements
      int[] userVals = new int[NUM_ELEMENTS]; // User numbers
      int i;                                  // Loop index
      int sumVal;                             // For computing sum

      // Prompt user to populate array
      System.out.println("Enter " + NUM_ELEMENTS + " integer values...");
      
      for (i = 0; i < userVals.length; ++i) {
         userVals[i] = scnr.nextInt();
         System.out.println("Value: " + userVals[i]);
      }

      // Determine sum
      sumVal = 0;
      for (i = 0; i < userVals.length; ++i) {
         sumVal = sumVal + userVals[i];
      }
      System.out.println("Sum: " + sumVal);

// FINDING MAXIMUM OF VALUE IN ARRAY //
-----------------------------------

for (i = 0; i < userVals.length; ++i) {
   if (userVals[i] > maxVal) {
    maxVal = userVals[i];
}
}
System..out.println("Max: " + maxVal);

ACCESING OIUT OF RANGE ARRAY ELEMMENT 

// SWAPPING VARIABLES //
To swap two variable s, you need a TEMPORARY VARIABLE to briefly store a value. 

int X = 33;
int Y = 55; 
int tempVal = 0;

tempVal = X; ***
X = Y;
Y = tempVal; 

// COPYING AN ARRAY //

for (i = 0; i < userVals.length; ++i) {
copiedVals[i] = userVals[i];
}
// REVERSING AN ARRAY //
for (i = 0; i < userVals.length; ++i) {
userVals[i] = userVals[userVals.length - 1 - i];


TWO-DIMENSIONAL ARRAYS //

int[][] myArray = new int[R][C]; 

ENHANCED FOR LOOPS: ARRAYS //

The enahnced for loop is a for loop that iterates through each elemnt in an array.

String[] teamRoster = new String[3]; 

   MORE EFFICIENT VERSTION FOR ENHANCED FOR LOOP
      for (String playerName : teamRoster) {
         System.out.println(playerName);

